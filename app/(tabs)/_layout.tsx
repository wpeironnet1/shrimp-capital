import { Ionicons } from '@expo/vector-icons';
import { Tabs } from 'expo-router';
import { colors } from '../../src/theme/colors';
export default function TabLayout() { return <Tabs screenOptions={({ route }) => ({ headerShown: false, tabBarActiveTintColor: colors.coral, tabBarInactiveTintColor: colors.muted, tabBarStyle: { position: 'absolute', backgroundColor: '#09242D', borderTopColor: '#214752', height: 82, paddingTop: 8 }, tabBarLabelStyle: { fontWeight: '800', fontSize: 10, marginBottom: 8 }, tabBarIcon: ({ color, size }) => <Ionicons name={route.name === 'index' ? 'fish' : route.name === 'market' ? 'trending-up' : 'people'} color={color} size={size} /> })}><Tabs.Screen name="index" options={{ title: 'Farm' }} /><Tabs.Screen name="market" options={{ title: 'Market' }} /><Tabs.Screen name="clan" options={{ title: 'Clans' }} /></Tabs>; }
